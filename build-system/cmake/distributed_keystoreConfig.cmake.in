@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(UNIX)
  find_dependency(Threads)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/distributed_keystoreTargets.cmake")

check_required_components(distributed_keystore)